package com.sys.bio.back.repositories.sized;

import com.sys.bio.back.models.sized.StrawType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StrawTypeRepository extends JpaRepository<StrawType, Long> {
}
